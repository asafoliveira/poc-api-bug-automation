{
  "name": "automated-api-defect-reporter",
  "version": "1.0.0",
  "description": "PoC: criação automática de bugs a partir de falhas em testes de API (Playwright + Jira + template)",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "process-failures": "tsx scripts/processFailures.ts"
  },
  "dependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
